(1) The Dockerfile defines a linux instance which has all of the dependencies specified in AutoDot's main README. Upon running, user enters the shell of this linux instance. At this point the user can invoke the python shell and run commands as per the Playground directories README.
    BUILD COMMAND: docker build -t my-python-app .
    RUN COMMAND: docker run -it my-python-app
